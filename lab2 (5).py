# Введення числа
n = int(input("Введіть натуральне число n (до 9999): "))

# Перетворення в рядок з 4 символами (додаємо ведучі нулі)
n_str = str(n).zfill(4)

# Перевірка унікальності цифр
is_unique = len(set(n_str)) == 4

# Вивід результату
print(is_unique)